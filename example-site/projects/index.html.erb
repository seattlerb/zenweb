---
title: example.com projects
layout: site
---

Sometimes I write codes and then share them on the internet. Here are
some of those codes.

<h1 class="section">Projects</h1>
<ul id="post_list" class="activity">
    {% for post in site.categories.projects %}
    <li>
        <h2><a href="{{ post.url }}">{{ post.title }}</a></h2>
        <h3 class="date">created {{ post.date.clean }}</h3>
        <p>{{ post["tagline"] }}</p>
    </li>
    {% end %}
</ul>
